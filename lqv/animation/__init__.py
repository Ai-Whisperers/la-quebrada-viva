"""Animation package — dormant.

Currently the deliverable is 18 still frames. Future motion deliverables
(turntable demo for the escritura presentation, flythrough of the housing
park) will land here. Adding modules to this package does not affect the
still-frame render path: ``build_scene.py`` does not import ``lqv.animation``.
"""
